<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_gravity="start">
    <item
        android:id="@+id/search_word"
        android:icon="@drawable/ic_searchword"
        android:title="@string/search"
        app:showAsAction="ifRoom">
    </item>

    <item
        android:id="@+id/bookmark"
        android:icon="@drawable/app_bookmark_add"
        android:title="@string/app_add_bookmark"
        app:showAsAction="ifRoom">
    </item>

    <item
        android:id="@+id/share"
        android:icon="@drawable/ic_share"
        android:title="@string/app_share"
        app:showAsAction="ifRoom">
    </item>

    <item
        android:id="@+id/save_rule"
        android:icon="@drawable/ic_save_toolbar"
        android:title="@string/app_save"
        app:showAsAction="ifRoom">
    </item>
</menu>